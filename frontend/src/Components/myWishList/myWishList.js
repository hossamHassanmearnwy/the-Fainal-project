import React from 'react'
// import Card from 'react-bootstrap/Card';
// import { useSelector } from 'react-redux'
// import { useDispatch } from 'react-redux'
// import { removeFromWishList } from '../../store/actions/wishListAction'

export default function MyWishList(){
  // const favorites = useSelector(state => state.favorite.favorites)

  return (<>
  <div>myWishList</div>
   {/* <h1>{favorites.length}</h1>
   {favorites.map((product,index)=>{
     return <Card style={{ width: '18rem' }} key={index} className="mt-5"> */}
       {/* <i className="bi bi-trash3" onClick={() => dispatch(removeFromWishList(product))} ></i> */}
       {/* <Card.Img variant="top" src={`https://image.tmdb.org/t/p/w500/${product.poster_path}`} /> */}
       {/* <Card.Img variant="top" src={`url`} />

       <Card.Body>
         <Card.Title>{product.title}</Card.Title>
         <Card.Text>
           {product.price}
         </Card.Text>

       </Card.Body>

     </Card> */}

  {/* //  })} */}
  
  
  
  
  </>
  )
}
